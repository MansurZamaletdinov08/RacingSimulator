#include "Distance.h"


/*
int Distance(int distance) {
	int value;
	if (distance < 1000) {
		value = distance;
		return 1;
	}

	if (distance < 5000 || distance > 1000) {
		value = distance * 0.97;
		return 2;
	}

	if (distance < 10000 || distance > 5000) {
		value = distance * 0.90;
		return 3;
	}

	if (distance >= 10000) {
		value = distance * 0.95;
		return 4;
	}
	return value;
}
*/